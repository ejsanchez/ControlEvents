require File.dirname(__FILE__) + '/../test_helper'
require 'consult_controller'

# Re-raise errors caught by the controller.
class ConsultController; def rescue_action(e) raise e end; end

class ConsultControllerTest < Test::Unit::TestCase
  def setup
    @controller = ConsultController.new
    @request    = ActionController::TestRequest.new
    @response   = ActionController::TestResponse.new
  end

  # Replace this with your real tests.
  def test_truth
    assert true
  end
end
